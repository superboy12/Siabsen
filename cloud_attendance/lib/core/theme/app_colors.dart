import 'package:flutter/material.dart';

class AppColors {
  // Primary Navy Blue
  static const Color primary = Color(0xFF0F2C59);
  static const Color primaryLight = Color(0xFF1E4885);
  static const Color primaryDark = Color(0xFF071936);

  // Backgrounds
  static const Color background = Color(0xFFF8F9FA); // Light Grey
  static const Color surface = Colors.white;

  // Texts
  static const Color textPrimary = Color(0xFF212529);
  static const Color textSecondary = Color(0xFF6C757D);

  // Accents & States
  static const Color success = Color(0xFF198754); // Green
  static const Color error = Color(0xFFDC3545); // Red
  static const Color warning = Color(0xFFFFC107); // Yellow

  // Borders
  static const Color border = Color(0xFFDEE2E6);
}
